# Nhật ký review & fix output của AI

Mỗi dòng được ghi **ngay tại thời điểm phát hiện và sửa**, không dựng lại vào cuối bài - cột "Vì sao AI trượt" không tái tạo được sau khi đã quên bối cảnh.

**Sinh viên:** Lý Quốc Thạnh - 23127262

Công cụ AI: **Claude Opus 5** (Claude Code CLI).

---

| # | Ngày giờ | File:vị trí | AI sinh gì | Sai chỗ nào | Mình sửa thành | Vì sao AI trượt |
|---|---|---|---|---|---|---|
| 1 | 2026-08-11 09:15 | `scripts/monitor.sh:16` | `PID=$(pgrep -f "node server.js" \| head -1)` để tìm tiến trình backend cần đo tài nguyên | `pgrep -f` so khớp **toàn bộ dòng lệnh**, nên mẫu không neo đã khớp trúng tiến trình `bash -c` bao ngoài - dòng lệnh của nó có chứa chuỗi `node server.js`. Kết quả: toàn bộ 332 mẫu tài nguyên của lượt Load đầu ghi nhầm PID 44059, cho ra **RSS 2.1 MB / CPU 0.0%**, trong khi backend thật là PID 44060 với **RSS 117 MB / CPU 2.2%** | Neo hai đầu mẫu: `pgrep -f '^node server\.js$'`, và thêm chốt chặn từ chối PID nào có RSS dưới 30 MB vì tiến trình Node thật không bao giờ nhỏ vậy | **Đặc điểm môi trường, không phải giới hạn model.** Mẫu `pgrep -f "node server.js"` là cách viết chuẩn và chạy đúng ở hầu hết bối cảnh; nó chỉ hỏng vì lệnh được bọc trong `bash -c` - thứ chỉ tồn tại trong phiên chạy qua công cụ. AI không có cách nào biết dòng lệnh cha sẽ chứa lại chính chuỗi đó. Cái đáng trách hơn là **AI không tự nghi ngờ kết quả**: RSS 2.1 MB cho một tiến trình Node phục vụ 45 req/s là bất khả thi về mặt vật lý, lẽ ra phải có bước kiểm tra tính hợp lý ngay trong script - nên bản sửa bổ sung luôn chốt chặn đó |

| 2 | 2026-08-11 09:30 | `scripts/monitor.sh:34` | Lấy mức chiếm CPU bằng `ps -p $PID -o %cpu=` | `%cpu` của `ps` là **trung bình cộng dồn từ lúc tiến trình khởi động**, không phải mức tức thời. Backend đã chạy rảnh 20 phút trước khi bắn tải, nên một đợt tải 5 phút bị pha loãng và cột CPU đứng phẳng ở `2.1%` suốt cả lượt chạy - con số này mà đưa vào báo cáo là tự tạo ra một lỗi diễn giải metric, đúng loại lỗi mà Task 2 yêu cầu đi săn | Đọc trực tiếp `utime + stime` từ `/proc/<pid>/stat`, lấy hiệu giữa hai lần lấy mẫu rồi chia cho `CLK_TCK` và chu kỳ. Kiểm chứng: bắn 300 request liên tiếp thì cột CPU nhảy `0% -> 29% -> 17% -> 0%`, trong khi `ps %cpu` vẫn đứng yên `2.1%`. Thêm luôn `vsz_kb` và `open_fds` để theo dõi rò rỉ tài nguyên | **Giới hạn model kiểu "mặc định trôi theo thói quen".** `ps -o %cpu` là cách viết phổ biến nhất khi hỏi "đo CPU của tiến trình trong shell", và AI chọn đúng cái phổ biến đó mà không xét tới **ngữ cảnh đo lường** - rằng đây là bài kiểm thử hiệu năng, nơi phân biệt "tức thời" với "trung bình đời sống tiến trình" là bản chất chứ không phải chi tiết vặt. Prompt cũng không nêu rõ yêu cầu này, nên đây là lỗi kép: prompt thiếu ràng buộc và model không tự đặt câu hỏi về tính hợp lệ của đơn vị đo |

| 3 | 2026-08-13 07:19 | `plans/23127262_Stress_20260813.jmx` - Response Assertion nhánh thăm dò lockout | Assertion kiểm mã phản hồi khớp cột `expect_code` (`401\|403`), để `Assertion.assume_success = false` như mọi assertion khác trong plan | Assertion **pass** nhưng JMeter vẫn đánh sample là thất bại. JMeter mặc định coi mọi HTTP 4xx là lỗi ở tầng sampler, trước cả khi assertion chạy; assertion pass không ghi đè được. Với nhánh thăm dò lockout thì 401/403 là kết quả **mong đợi**, nên error rate của cả kịch bản bị thổi lên bằng chính những phản hồi đúng ý đồ. Bản smoke đầu: 30/30 sample probe bị tính lỗi trong khi cả hai assertion đều `failure=false` | Bật `Assertion.assume_success = true` (Ignore Status) **chỉ trên nhánh probe**, giữ `false` ở nhánh hợp lệ để lỗi thật vẫn được đếm. Sau khi sửa: 20 sample 401 + 10 sample 403 đều `success=true`, error rate về đúng 0% | **Đặc điểm công cụ mà AI mô hình hoá thiếu.** AI hiểu đúng ý định nghiệp vụ (401/403 là mong đợi) và viết assertion đúng, nhưng bỏ qua việc JMeter có **hai tầng phán quyết độc lập**: trạng thái sampler và kết quả assertion. Đây là kiến thức đặc thù công cụ, không suy ra được từ logic nghiệp vụ. Điểm đáng trách là AI đã tự sinh ra một kịch bản mà "phản hồi lỗi là kết quả đúng" rồi lại không rà đúng cơ chế xử lý trường hợp đó - lẽ ra khi thiết kế nhánh negative-testing thì Ignore Status phải là bước phản xạ |
| 4 | 2026-08-13 07:20 | `CLAUDE.md` R13 - quy trình reset lockout | Đề xuất hai lệnh reset giữa các lượt chạy: `rm sut/backend/database.sqlite && node sut/backend/database.js`, hoặc `sqlite3 ... "UPDATE users SET login_attempts=0"` | Cả hai đều hỏng trong thực tế. (a) `sqlite3` CLI **không được cài** trên máy này. (b) Nguy hiểm hơn: `sut/backend/database.js` gọi `initDatabase()` ngay khi được import (dòng 117), và hàm này mở đầu bằng **6 lệnh `DROP TABLE`** (dòng 15-20). Nghĩa là chỉ cần `require` file đó là mất sạch 200 tài khoản + 147 sản phẩm do `seed-data.js` tạo. Tôi đã tự dính bẫy này **hai lần** khi dùng `node -e "require('./database')"` để *đọc* trạng thái DB - lệnh chẩn đoán tưởng chỉ đọc lại xoá sạch dữ liệu nền, và lượt smoke sau đó cho 670 sample `HTTP 401` vì toàn bộ tài khoản đã biến mất | Viết `scripts/reset-lockout.js` mở thẳng `database.sqlite` bằng driver `sqlite3` trong `node_modules`, **không import `database.js`**, chỉ chạy `UPDATE users SET login_attempts=0, locked_until=NULL`. Kiểm chứng: sau khi reset còn nguyên 212 tài khoản. Đã sửa lại R13 trong `CLAUDE.md` | **Lỗi do đọc tài liệu thay vì đọc mã nguồn.** `setup_guide.md` của SUT giới thiệu `node backend/database.js` là "lệnh reset dữ liệu", và AI chép lại nguyên văn vào quy tắc thường trực mà không mở file ra xem nó làm gì. Sai lệch chí mạng nằm ở chỗ file này vừa là **module** vừa là **script**: import nó để dùng `db` cũng kích hoạt luôn tác dụng phụ huỷ dữ liệu. Bài học rút ra và đã áp dụng: mọi lệnh được ghi vào quy tắc thường trực phải được **chạy thử và kiểm chứng hậu quả**, không chép từ tài liệu |
| 5 | 2026-08-13 07:44 | Vòng lặp lấy mẫu CPU của JMeter (lệnh tạm, không lưu vào repo) | `while pgrep -f "ApacheJMeter.jar"; do ... done` để theo dõi CPU của chính JMeter, nhằm xác định nút thắt nằm ở SUT hay ở công cụ đo | Vòng lặp **tự khớp chính nó**: dòng lệnh của subshell có chứa chuỗi `ApacheJMeter.jar`, nên `pgrep -f` trả về PID của subshell thay vì tiến trình Java. Kết quả ghi được là `CPU=0%, RSS=4MB` - vô nghĩa. Đây là **tái phát đúng lỗi số 1**, chỉ khác tiến trình | Đổi sang `pgrep -x java`. Và vì `ps %cpu` là trung bình cộng dồn (lỗi số 2), kết luận về nút thắt được rút từ hai nguồn đo đúng: CPU tức thời của `node` từ `/proc/<pid>/stat` (trung bình 102%, đỉnh 132%) và `loadavg` 1 phút (1,53-1,93 trên máy 16 nhân ~ 12% toàn máy) | **Cùng một lỗi lặp lại vì cùng một điểm mù.** Lỗi số 1 đã được ghi vào nhật ký này từ 2 ngày trước, vậy mà khi cần một lệnh `pgrep` mới, AI vẫn viết lại đúng khuôn cũ. Điều này cho thấy nhật ký chỉ có giá trị nếu được **đọc lại trước khi sinh artifact cùng loại**, chứ bản thân việc đã ghi nó xuống không ngăn được tái phạm - một giới hạn thật của cách làm việc với AI, không phải chi tiết kỹ thuật |

---

## Ghi chú phương pháp

Số liệu tài nguyên của lượt Load đầu tiên (`load-20260811T022251Z-resource.csv`) đã bị **huỷ bỏ, không dùng trong báo cáo**. Lượt chạy đã được thực hiện lại sau khi sửa script. File `.jtl` của lượt đầu vẫn hợp lệ vì lỗi chỉ nằm ở khâu đo tài nguyên, không đụng tới phép đo độ trễ.
